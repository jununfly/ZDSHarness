# Handoff — ZHarness 使用期立项（2026-08-17）

> 交接范围：ZHarness workspace（本仓库）。外部项目（ZBrain、AI 研究报告产品等）不在本图范围内，见「范围原则」。

## 一句话现状

ZHarness 学习期收官（roadmap 1-1~1-5 全完成），使用期节点 1-6 已开启；**1-6-1 真实 LLM e2e 已验收关闭**（证据落盘，commit `ab199c8` 已 push）；当前 frontier 停在 **1-6-3 第一个项目立项的 grilling 进行到一半**——Q1/Q2 已定，Q3/Q4 待用户回答。

## 本 session 完成的事

1. **skills 重装**：ZAgentic 全套 46 个 `zj-*` skills 装入 `~/.workbuddy/skills/`（copy 快照模式，非 symlink）。
2. **roadmap 扩展**（grilling Round 1，8 问全同意推荐）：
   - 新增 1-6「使用期：从学习转入构建」+ 三子节点。
   - 学习定位=手段；deferred 项显式建节点（1-6-2 llm 按需精读 pending）。
3. **1-6-1 真实 LLM e2e 完成并关闭**（Round 2 决策 + 执行）：
   - 辅验收：`packages/llm/llm-deepseek/tests/adapter.e2e.ts` 6/6 通过（真实 API，含 tool-call round trip + reasoning 回传）。
   - 主验收：`pnpm dsh --profile headless "统计本仓库 README.md 的英文词数并报告"` 成功，session 158 事件：5 次 tool/call+result 回传、6 条 assistant message、6 step 闭环。
   - session 证据：`~/.dsh/sessions/--Users-bilibili-Documents-workspace-github-jununfly-ZHarness--/session-5b88db26-a5cf-4bb2-9ba0-24842284e360/session.jsonl.zstd`（zstd 压缩 jsonl）。
   - 4 条 decide 已写入 roadmap json，见 `docs/zj/learning_roadmap.json` 节点 1-6-1。

## 立即要做：继续 1-6-3 grilling（Round 3 进行中）

已定：
- **Q1 = (d)** 完整小型 demo agent（基于 SDK + ACP/JSON-RPC 端到端）。
- **Q2 = (ii) 生产型交付**（非 spike）：测试=契约+e2e 双层、有文档、用户实际反复用。

待用户回答（按 zj-grilling 风格追问，先查事实再问）：
- **Q3 主题**：候选 α 研究材料聚合 agent（推荐）/ β skills 仓库漂移巡检 / γ 路线图复盘。关键质询点：用户日常是否真会反复用——生产型的前提。
- **Q4 放置与验收**：推荐 own package under `packages/examples/<name>`；首个切片=骨架可启动+一次空 turn+契约测试红绿；验收与 1-6-1 对齐（session 证据）。

Q3/Q4 答完后：decide 落盘 → 1-6-3 转 in_progress → render md → 开始施工（TDD、小切片）。

## 范围原则（zj 确立，必须遵守）

ZHarness 路线图范围**严格限定在 ZHarness 项目本身**。外部项目（ZBrain TS master / rust-rewrite、AI 研究报告产品 chat）即使有交集也不混入本图，在各自 workspace 独立跟踪。判定：对话在哪个 workspace，默认项目上下文就是哪个。本 session 曾犯过此错（把 ZBrain 现状拉进 Round 3 质询），已纠正并写入 `.workbuddy/memory/MEMORY.md`。

## 环境与工具事实（省掉重新踩坑）

- **pnpm 不在系统 PATH**：用隔离 shim——`export PATH=~/.workbuddy/binaries/node/workspace/shims:~/.workbuddy/binaries/node/versions/22.22.2/bin:$PATH`（corepack enable 产物，COREPACK_HOME=`~/.workbuddy/binaries/node/workspace/.corepack`）。pre-push typecheck 也需要它。
- **`.env`**：仓库 app-boot 与 vitest.e2e 只读**仓库根** `.env`（不读 `~/.env`）。根 `.env` 已就位（gitignored，含 DEEPSEEK_API_KEY——勿外泄）。
- **dsh 走 built `lib/`**：首次/改动后需 `pnpm run build`，否则 typert-loader 报 Cannot find module。
- **stale lock 两处**：`.git/dsh-lefthook-install.lock`（pnpm install 偶发残留，直接 rm）；roadmap `.json.lock`（被中断的 roadmap_cli 残留，`kill -0 <pid>` 验死再 `roadmap_cli.py unlock`）。
- **roadmap_cli 无 `complete` 命令**：用 `update <json> <id> --status completed`。
- **session 校验脚本模式**：zstandard 流式解压 `session.jsonl.zstd` → 按 `type` 聚合事件、提取 `tool/call`+`tool/result`（python venv：`~/.workbuddy/binaries/python/envs/default`，已装 zstandard）。

## 相关工件索引（不复制，按路径引用）

- 路线图 SoT：`docs/zj/learning_roadmap.json`（render 目标 `docs/zj/learning_roadmap.md`，render 前先删 md——zj 约定）
- roadmap CLI：`~/.workbuddy/skills/zj-roadmap-driven/roadmap_cli.py`（python：`~/.workbuddy/binaries/python/versions/3.13.12/bin/python3`）
- 学习期产出：`docs/zj/architecture_panorama.md`（L1-L6）、`docs/zj/cordis-subsystem*`、`docs/zj/notes/`、`docs/zj/upstream-compare.md`
- 当日日志：`.workbuddy/memory/2026-08-17.md`；长期记忆：`.workbuddy/memory/MEMORY.md`
- 仓库约定：`AGENTS.md`、`docs/AGENTS.md`、pre-push 检查 skill `.agents/skills/dsh-pre-push-checks/SKILL.md`

## Git 状态

- 已 push：`ab199c8` docs(roadmap): close learning phase, open usage phase 1-6 and complete 1-6-1 real LLM e2e（master → origin/master）。
- 工作树其余干净；`vendor/cordis/bin.js` 的 mode-only 噪音已还原；`scripts/check-vendor-manifest.sh` 曾需 `chmod +x`。
- 本文档（handoff md）未 commit——按约定等显式指令。

## Suggested skills（下一个 agent 应加载）

1. `zj-roadmap-driven` — 路线图读写/渲染/frontier 协议（继续 1-6-3 必备）。
2. `zj-grilling` — 完成 Round 3 剩余质询（Q3/Q4）。
3. `dsh-pre-push-checks`（仓库 `.agents/skills/`，见 AGENTS.md）— 施工期 push 前检查。
4. `zj-tdd` — 1-6-3 施工期红绿循环。
